# FinanceLake Terminal Configuration

# --- 1. Infrastructure Paths ---
# Base directory for local storage, adjust as needed
BASE_PATH=/tmp/financelake
BASE_PATH_FILE=file:///tmp/financelake

# UI Assets
LOGO_PATH=resources/img/logo.png

# Medallion Architecture Paths (Dynamic Expansion)
BRONZE_PATH=${BASE_PATH_FILE}/bronze
SILVER_PATH=${BASE_PATH_FILE}/silver
GOLD_FEATURES_PATH=${BASE_PATH_FILE}/gold/features
GOLD_PREDICTIONS_PATH=${BASE_PATH_FILE}/gold/predictions

# Streaming Checkpoints
BRONZE_CHECKPOINT=${BASE_PATH_FILE}/bronze_checkpoint
SILVER_CHECKPOINT=${BASE_PATH_FILE}/silver_checkpoint
GOLD_PROCESSING_CHECKPOINT=${BASE_PATH_FILE}/gold_processing_checkpoint

# --- 2. Kafka Configuration ---
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_TOPIC=store_prices

# --- 3. Machine Learning Model ---
# Path for saving/loading the Spark ML model
# (Defaulting to /tmp for local development; adjust as needed)
MODEL_PATH=${BASE_PATH_FILE}/models/random_forest_classifier
MODEL_OUTPUT_PATH=${BASE_PATH_FILE}/models/random_forest_classifier

# --- 4. External Data Sources ---
# Finnhub.io API Key
# Finnhub is free, get your own API key at https://finnhub.io/
# this key is public
FINNHUB_API_KEY=d4694k1r01qj716fv2tgd4694k1r01qj716fv2u0